// Generated by CoffeeScript 1.9.2
(function() {
  var app;

  app = angular.module("RegisterPage", []);

  app.controller("RegisterController", [
    "$scope", function($scope) {
      $scope.register = function() {
        return $.post("/account", {
          username: $scope.username,
          password: $scope.password
        }, function(data) {
          if (data.error != null) {
            return console.log(data.error);
          } else if (data.message != null) {
            console.log(data.message);
            return window.location.replace("http://" + window.location.host + "/login");
          }
        });
      };
      return $scope.update = function() {
        if ($scope.password === "" && $scope.passwordConfirm === "") {
          return $scope.confirmStyle = "";
        } else if ($scope.password === $scope.passwordConfirm) {
          return $scope.confirmStyle = "green";
        } else {
          return $scope.confirmStyle = "red";
        }
      };
    }
  ]);

}).call(this);
